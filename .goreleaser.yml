builds:
  - binary: goci-sample
    ldflags:
      - -s -w
    goos:
      - linux
    goarch:
      - amd64
      - arm64
    env:
      - CGO_ENABLED=0
archives:
  - format: zip
    name_template: "{{ .ProjectName }}_{{ .Os }}_{{ .Arch }}"
    rlcp: true
#release:
#  github:
#    owner: ryuichi1208
#    name: goci-sample
